@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "ورود با کد بازیابی";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-6 mx-auto col-sm-12">
        <h1>@ViewData["Title"]</h1>
        <hr />
        <div class="alert alert-info">
            <p>
                شما درخواست ورود با کد بازیابی کرده اید. این ورود کد احراز هویت دو مرحله ای شما را بازنشانی نخواهد کرد.
                با این حال، ورود با کد بازیابی امنیت حساب کاربری شما را کاهش نخواهد داد. اگر احساس می‌کنید که حساب کاربری شما به خطر افتاده است،
                باید <a asp-controller="Manage" asp-action="GenerateRecoveryCodes">کدهای بازیابی جدید</a> ایجاد کنید.
            </p>
        </div>
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ReturnUrl" />
            <div class="mb-3">
                <label asp-for="RecoveryCode" class="form-label"></label>
                <input asp-for="RecoveryCode" class="form-control" autocomplete="off" placeholder="مثال: ABCD-EFGH" />
                <span asp-validation-for="RecoveryCode" class="text-danger"></span>
                <small class="form-text text-muted">
                    کد بازیابی خود را بدون فاصله یا خط تیره وارد کنید.
                </small>
            </div>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">
                    <i class="fa-solid fa-right-to-bracket"></i> ورود
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
